export class EncontrarPedidoById {
    campos: Object;
    relaciones: string[];
    constructor(
        campos: Object,
        relaciones: string[]
    ){
        this.campos = campos;
        this.relaciones = relaciones;
    }
}